# 安全边界

- 本服务只用于本机或隔离 VM，监听 `127.0.0.1`，不是公网服务器。
- 正常认证路径实际验证密码，再发放会话、区服票据并绑定 Windows 进程身份。
- `--offline-client-adapter` 是显式免认证调试模式，禁止当作正常登录方案。
- 本地管理员可改数据库、进程或脚本，不在本威胁模型保护范围内。
- 不记录认证正文、聊天正文、密码、会话令牌、查询串或私钥；实验日志也应先检查再分享。
- 数据库包含账号和认证信息，不要提交；SQLite 运行时可能同时产生 WAL/SHM 文件。
- SDK 内存公钥必须来自本轮自建服务；服务重启需要重新准备客户端适配，不使用旧服务公钥。
- 适配模块修改当前进程的导入/事件分发或 SDK 状态。只允许授权的隔离客户端副本；不要禁用主机安全软件或加载旧反外挂驱动。
- 本仓库不包含任何实际口令文件或自动登录的保存凭据。若需本地保存，请使用系统凭据保护并单独设置访问权限。
- 漏洞报告只提供最小合成复现，不在公开 issue 上传口令、账号数据库、完整客户端或转储。

源代码文本扫描和测试不能证明不存在所有敏感信息或安全缺陷。部署前仍需独立审查。
