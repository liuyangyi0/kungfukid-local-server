# 安全边界

- 默认及账号/原SDK/native模式仅监听 `127.0.0.1`。显式`--mode lab --insecure-host-only-lab`允许严格私网与指定对端的双VM夹具，不进行真实密码认证；所有模式都不是可直接公网发布的服务器。
- 正常认证路径实际验证密码，再发放会话、区服票据并绑定 Windows 进程身份。
- `--offline-client-adapter` 是显式免认证调试模式，禁止当作正常登录方案。
- 本地管理员可改数据库、进程或脚本，不在本威胁模型保护范围内。
- 不记录认证正文、聊天正文、密码、会话令牌、查询串或私钥；实验日志也应先检查再分享。
- 数据库包含账号和认证信息，不要提交；SQLite 运行时可能同时产生 WAL/SHM 文件。
- SDK 内存公钥必须来自本轮自建服务；服务重启需要重新准备客户端适配，不使用旧服务公钥。
- 适配模块修改当前进程的导入/事件分发或 SDK 状态。只允许授权的隔离客户端副本；不要禁用主机安全软件或加载旧反外挂驱动。
- 本仓库不包含任何实际口令文件或自动登录的保存凭据。若需本地保存，请使用系统凭据保护并单独设置访问权限。
- 公开宿主模板不执行需要VM用户名/密码的guest operations，不把密码放进`vmrun`参数；游戏启动改在VM已登录桌面执行。
- `kk-roleprop-ready.txt`只有指针文本；`ReadyFile`仅检查时间戳和指针范围，不绑定PID、进程创建时间或运行ID。启动器移走旧文件、认证层检查进程身份是另外两层措施，不能合称强绑定就绪证明。不要把此文件当安全授权票据。
- 漏洞报告只提供最小合成复现，不在公开 issue 上传口令、账号数据库、完整客户端或转储。

源代码文本扫描和测试不能证明不存在所有敏感信息或安全缺陷。部署前仍需独立审查。

## Native 加密候选（2026-09-22）

`--mode native`使用TLS账户API下发短期随机授权密钥，SDK、游戏TCP和UDP在AES-256-GCM记录内传输；没有明文降级。旧模式不因此自动获得加密。新路径仍只绑定回环地址，`public_ready=false`。

加密层是自建设计，不是旧服务端算法，也没有通过独立密码协议审计；不另加应用层前向保密握手。真实SDK的晚加载模块、既存函数指针、异步网络调用覆盖还需原客户端验证。不能靠删除地址保护直接公网发布。

出站限制工具仅适用于专用VMware实验客体，Apply需管理员和关闭游戏。它会禁用整个客体既有的出站允许规则（包括DNS/更新等），不是普通桌面的一键优化；先Plan、保存回滚收据，按需显式Restore。此源码发布没有对任何实际机器应用策略。

测试中的固定key、nonce、密码和账号均为合成夹具，不得当部署密钥。不要上传实际TLS私钥、授权响应、VM凭据、账号数据库或流量捕获。
