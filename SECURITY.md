# 安全说明

## 支持范围与威胁模型

当前公网入口是显式的 `public` 模式，面向非排位测试。所有客户端均不可信，包括拥有合法账号、能生成有效密文的客户端。

服务端依次验证 TLS 账号认证、一次性区服授权、独立通道凭据、资源归属、阶段与房间权限。客户端程序、PID、文件摘要或就绪文件不能证明用户可信，也不能替代服务端认证。

旧本地调试模式仍可能存在免认证能力。它们不属于公网入口，不能仅修改监听地址后对外提供服务。健康与管理能力默认仅本机，数据库和私钥不得通过网页目录或游戏端口公开。

## 已有保护与边界

- 密码使用现有 scrypt 参数存储；邀请码只保存摘要并事务消费。
- TLS 用于认证；SDK/游戏 TCP 与 UDP 使用 `kk-aesgcm-v1` 认证加密及防重放，不允许明文回退。
- 连接、未认证请求、业务收发、队列和昂贵计算受预算限制。
- 资产操作校验价格、余额、归属与资格，相关修改在事务中完成。
- 公网非排位模式不依据客户端上报的战斗结果增加永久金币、经验、任务、排名或物品。
- 未知或未准入操作不应伪造成功或直接广播。部分战斗消息按明确策略准入，不意味着所有状态和特殊模式都已实现。

本服务尚未实现完整权威战斗，不保证杜绝外挂或公平竞技。加密层为自建设计，未经过独立密码协议审计，也没有另外实现应用层前向保密握手。资源限制不能抵御任意规模的链路级 DDoS。

配置检查、自动测试与进程就绪不等于生产安全认证。部署者还需核对证书、文件权限、端口、备份恢复和上游防护。

## 凭据与日志

不要提交或公开：密码、邀请码、会话/通道票据、私钥、账号数据库及 WAL/SHM、原始认证报文、完整流量或内存转储。

仅向客户端分发必要的公开证书，不分发服务端私钥。禁止用“接受所有证书”的回调解决连接错误。

测试中的账号、密码、key 和 nonce 是合成夹具，不可作为默认部署凭据。错误日志应使用有限类别与计数，分享前检查是否带有账号、聊天、背包、路径或认证正文。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability**（如果维护者已启用私密报告）。

若该入口不可用，可以开一个仅请求私密联系方式的 issue，说明受影响模块和大致风险；不要公开利用细节、真实凭据或原始数据，等待维护者提供安全渠道。

报告建议包含提交 ID、受影响运行模式、最小合成复现、预期/实际权限边界和潜在影响。不要对未获授权的公网服务进行测试。

若意外泄露凭据，应先在对应服务撤销或轮换。删除当前文件或提交修复不能收回已公开的历史与他人副本。

更多运行边界见 [SERVICE](docs/SERVICE.md) 与[部署运维](server/deploy/README.md)。
