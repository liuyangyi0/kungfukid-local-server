# 贡献指南

欢迎改进服务端业务、协议兼容、客户端版本适配、登录器、测试和文档。请用小而可验证的改动提交 PR，不要把格式整理、协议恢复和玩法修改混在同一批。

## 开始前

1. 阅读 [README](README.md) 的功能边界和支持版本，再看 [SERVICE](docs/SERVICE.md) 中对应业务。
2. 搜索已有 issue 和 PR。涉及新协议、新客户端版本或架构变更时，先开 issue 说明目标、依据和影响范围。
3. Fork 仓库，创建独立分支；不要提交数据库、构建产物或本地配置。
4. 当前仓库未指定 LICENSE。贡献前确认你有权提交相关代码；需要明确使用或再分发许可时，先与维护者讨论，不能把公开可见视作授权。

## 建立开发环境

服务端需要 Python 3.12。从仓库根目录执行：

```shell
python -m pip install -r requirements.txt
python -m unittest discover -s server/tests -p "test_*.py" -v
```

开发可使用本地虚拟环境。测试只访问临时数据库和回环端点，不使用正在运行的游戏账号库，不向真实服务压测。

其他相关测试：

```shell
python -m unittest discover -s client-adapter/tests -p "test_*.py" -v
python -m unittest discover -s tools/resource-recovery -p "test_*.py" -v
```

C# 登录器需要 Windows x64 / .NET Framework 4.8；构建和测试见[登录器说明](tools/client-lab/native-cloud-ticket/README.md)。C++ 客户端适配必须使用匹配的 i686 工具链，不能因为系统为 64 位就将注入 DLL 编译成 x64。

## 改动应该放在哪里

| 改动 | 主要入口 |
|---|---|
| 启动、运行配置与准入 | `server/kk_local/app/` |
| 业务分派与协议处理 | `server/kk_local/handlers/` 及已有对应协议模块 |
| 数据持久化、事务与迁移 | `server/kk_local/storage/` |
| 服务回归测试 | `server/tests/` |
| 登录器界面 | `tools/client-lab/LauncherProgram.cs`、`LocalAccountWindow.cs` |
| 游戏凭据交接 | `tools/client-lab/NativeCloudHandoff.cs` |
| 加密套接字适配 | `tools/client-lab/native-cloud-ticket/` |
| 客户端构建检查 | `client-adapter/tools/inspect_client.py` |

优先扩展已有模块；不建立第二套账号、房间、资产或协议索引。

## 报告问题

请提供：

- 仓库提交 ID、操作系统和 Python/.NET 版本。
- 游戏 `Version.dat` 值；涉及兼容时提供相关模块版本、大小和摘要，不上传文件本体。
- 运行模式、所用功能、最短复现步骤、期望结果和实际结果。
- 脱敏后的错误类型、消息编号、方向、长度和发生阶段。
- 如果涉及双方不同表现，说明在哪一端出现、发生在登录/房间/战斗/结算哪个阶段。

截图先遮住账号、私有地址等信息。日志只提供必要的小段，删除口令、授权令牌、聊天、背包和认证正文；不要直接附完整抓包或运行记录。

## 新协议与业务修复

1. 说明消息方向、编号、长度、字段边界、允许阶段、发送者权限与回复语义。
2. 区分明确字段、已观察行为与自建设计。未知项应保留未知，不把推测写成旧服务端事实。
3. 先增加能复现问题的合成测试，再改实现。覆盖成功、畸形输入、错误阶段及权限/归属拒绝。
4. 修改资产时覆盖事务回滚和并发/重试边界；不能信任客户端提交的价格、UID、奖励或胜负。
5. 更新现有 `docs/SERVICE.md` 对应章节，不另建并行协议清单。

不要用“返回成功”“放宽所有字段”“吞掉异常”代替协议实现。存在解析器不代表该业务已安全启用。

## 新客户端版本适配

当前参考游戏构建是 `1.13.0.594`，但游戏版本号不能唯一标识 SDK 组合。具体支持情况见[兼容矩阵](docs/CLIENT_COMPATIBILITY.md#sdk-build-compatibility)。

- 先提交组件的非敏感身份信息，说明与已有组合的区别。
- 为新版本增加独立合同，核对架构、ABI、对象布局、线程要求和生命周期。
- 保留已有版本检查；匹配失败应在修改对象或导入表前停止。
- 提供合成测试，并分别记录正常登录、错误密码、重试、窗口重建及退出/重登的本地结果。
- 不得仅修改地址常量，让未知布局通过原版本检查；模型测试通过也不能写成该客户端已实测兼容。

原 EXE/DLL、资源、IDA/PDB、转储和原始 trace 不通过 issue/PR 提交。可以提交自己编写的适配源码、合成夹具和不含载荷的结论说明。

## PR 检查清单

- [ ] 一次只解决一个清晰问题，说明用户可见变化和兼容影响。
- [ ] 相关测试通过，写明实际命令、结果及未验证范围。
- [ ] 没有绕过认证、证书、授权、所有权、阶段或预算校验。
- [ ] 没有提交密码、邀请码、令牌、私钥、数据库/WAL/SHM、游戏资源或生成二进制。
- [ ] 配置或数据库结构变化有迁移与回退说明，不重置已有账号。
- [ ] 文档反映真实能力，不把测试数量当作完整功能或安全认证。

`PUBLIC_SOURCE_MANIFEST.json` 是发布清单，由维护者在合并发布时统一更新。普通贡献不要为无关文件刷新摘要。

安全问题按 [SECURITY.md](SECURITY.md) 报告；不要在公开 issue 放可利用的真实凭据或攻击现场数据。
